import styled from 'styled-components';

export const Container = styled.h2`
    font-family: 'Inter';
    font-weight: 700;
    font-size: 36px;
    line-height: 44px;
    text-transform: uppercase;
    color: #FFFFFF;
`;
